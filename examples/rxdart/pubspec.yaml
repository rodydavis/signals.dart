name: rxdart_example
description: An absolute bare-bones web app.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  collection: ^1.18.0
  rxdart: ^0.27.7
  signals:
    path: ../../packages/signals

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.0
  lints: ^3.0.0
