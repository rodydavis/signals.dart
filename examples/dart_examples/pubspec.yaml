name: signals_dart_examples
description: Demonstrates Signals
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.1.0

dependencies:
  intl:
  collection:
  signals:
    path: ../../packages/signals
  sqlite_async: ^0.5.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
