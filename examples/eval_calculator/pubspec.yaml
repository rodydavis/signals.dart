name: eval_calculator
description: "A new Flutter project."
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  signals: ^3.0.0
  analyzer: ">=5.0.0"
  cupertino_icons: ^1.0.2
  material_table_view: ^3.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true