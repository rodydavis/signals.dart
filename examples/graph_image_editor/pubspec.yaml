name: graph_image_editor
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  signals:
    path: ../../packages/signals
  image: ^4.2.0
  file_picker: ^8.0.7
  file_saver: ^0.2.13
  dart_mappable: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.11
  dart_mappable_builder: ^4.2.3

flutter:
  uses-material-design: true
