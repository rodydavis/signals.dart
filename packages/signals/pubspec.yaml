name: signals
description: "signal, computed and effect built with dart and extensions for flutter"
repository: https://github.com/rodydavis/preact_signals.dart
version: 1.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_preact_signals: ^0.4.1
  preact_signals: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
