# extension `ActionExt5`

Extension on a 5-argument function to wrap it in a type-safe action.

---

## Members of `ActionExt5`

| Member | Type | Signature | Description |
| :--- | :--- | :--- | :--- |
| **action** | `method` | `R Function(A, B, C, D, E) action` | Wraps the 5-argument function in a type-safe action. |
