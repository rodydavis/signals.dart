# extension `ActionExt10`

Extension on a 10-argument function to wrap it in a type-safe action.

---

## Members of `ActionExt10`

| Member | Type | Signature | Description |
| :--- | :--- | :--- | :--- |
| **action** | `method` | `R Function(A, B, C, D, E, F, G, H, I, J) action` | Wraps the 10-argument function in a type-safe action. |
